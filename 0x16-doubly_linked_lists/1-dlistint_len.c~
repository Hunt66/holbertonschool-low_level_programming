#include "lists.h"

/**
 *dlistint_len- counts the nodes in a doubly linked dlistint_t
 *@h: head of dlistint_t
 *Return: the number of nodes
 */

size_t dlistint_len(const dilstint_t *h)
{
	size_t i;

	for (i = 0; h != NULL; i++)
		h = h->next;

	return (i);
}
