#include"holberton.h"
#include<stdio.h>

/**
 *main - prints out the name of the exicutable file
 *Return: 0 alwayse
 */

int main(int argc, char* argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
