#include "holberton.h"
/**
 *print_lines - accepts an int
 *             prints that num
 *             of _ chars
 *@n: accepted int
 *
 *Return: void
 */

void print_lines(int n)
{
	int i;

	i = 0;
	if (n >= 0)
		while (i <= n)
		{
			_putchar('_');
			i++;
		}
	_putchar('\n');
}
